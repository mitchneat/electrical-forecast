import { ToolProxy } from "./tool_proxy";
export class ToolGroup extends ToolProxy {
    static __name__ = "ToolGroup";
    constructor(attrs) {
        super(attrs);
    }
    static {
        this.define(({ Bool }) => ({
            show_count: [Bool, false],
        }));
    }
    get tooltip() {
        return this.tools.map((tool) => tool.tooltip).join("\n");
    }
}
//# sourceMappingURL=tool_group.js.map